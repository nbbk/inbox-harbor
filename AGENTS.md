# 项目模型路由

开发任务按风险分级，并使用 `.codex/agents/` 中定义的代理：

- 文件搜索、日志归纳、代码定位和只读探索：委派给 `explorer`。
- 常规功能实现、修复和测试：委派给 `implementer`。
- 解析器、数据库、复杂算法、并发、认证、安全边界和恢复逻辑：实现完成后委派给 `core_reviewer` 做只读审查。
- 发布、合并、交付，或用户明确要求完整验证时：最后委派给 `release_reviewer` 做发布门禁审查。

互不依赖的只读任务可以并行执行。避免让多个写代理同时修改同一文件或同一状态。主代理必须等待相关代理返回，整合结论并完成最终验证。

不要仅因为任务较长就升级模型；按风险、失败影响和验证证据升级。简单任务可以由主代理直接完成。如果指定模型在当前账户不可用，保留代理职责并改用可用模型，同时明确说明回退情况。

